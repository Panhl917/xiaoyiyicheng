// ============================================================
// 接口域名配置 —— 部署 / 发布时只需要改这里
// ============================================================
//
// 填写说明：
// 1. 必须是「已备案域名」的 HTTPS 地址（微信小程序强制要求）
// 2. 用标准 443 端口，不要带 :端口号（如 :8000 会被拒绝）
//    例：后端跑在本地 8000，需用 Nginx / 云托管把 443 的 HTTPS 反代到 8000
// 3. 结尾不要加斜杠
//
// 本地开发调试时可临时填局域网地址（需在小程序后台勾选「不校验合法域名」）：
//   'http://192.168.110.176:8000'
//
module.exports = {
  API_BASE_URL: 'https://api.yourdomain.com'
};
